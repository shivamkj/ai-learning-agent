OPENAI_ENDPOINT=https://api.openai.com/v1/
OPENAI_KEY="...."
OPENAI_MODEL="gpt-5.6-terra"
PASSWORD="your-secret-password"
PYTHON_BIN="../pdf-to-markdown/.venv/bin/python"

# PDF parser: "local" (Python docling) or "llamacloud" (LlamaCloud API)
PDF_PARSER="local"
# Required when PDF_PARSER=llamacloud
LLAMACLOUD_API_KEY="llx-..."
